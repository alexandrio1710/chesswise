# Runtime dependencies, pinned to exact versions for reproducibility.
# Install with: pip install -r requirements.txt
# (Stockfish itself is a separate binary — see README for install instructions,
# it is not a Python package. Redis is likewise a separate service, not a
# Python package — see celery_app.py's docstring.)
#
# Top-level: requests, chess, stockfish, fastapi, uvicorn, python-dotenv,
# celery, redis (client).
# Everything else below is their transitive dependencies, pinned so a fresh
# install can't silently drift to an incompatible version.

# --- Web platform: Section 4 (background analysis) ---
celery==5.6.3
redis==8.1.0
amqp==5.3.1
billiard==4.2.4
click-didyoumean==0.3.1
click-plugins==1.1.1.2
click-repl==0.3.0
kombu==5.6.2
vine==5.1.0

annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
charset-normalizer==3.4.9
chess==1.11.2
click==8.4.2
colorama==0.4.6
fastapi==0.141.1
h11==0.16.0
httptools==0.8.0
idna==3.18
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
PyYAML==6.0.3
requests==2.34.2
starlette==1.6.0
stockfish==5.2.0
typing-inspection==0.4.3
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn[standard]==0.52.1
watchfiles==1.2.0
websockets==17.0.1
